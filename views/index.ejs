<!DOCTYPE html>
<html>
<head>
  <title>Mini Message Board</title>
  <link rel="stylesheet" href="/styles.css">
</head>

<body>

  <h1>Messages</h1>

  <ul>
    <% messages.forEach((message) => { %>
      <li>
        <p><strong>User:</strong> <%= message.username %></p>
        <p><strong>Message:</strong> <%= message.message %></p>
        <p><strong>Time:</strong> <%= message.time %></p>
      </li>
      <a href="/open/<%= message.username %>" class="open-btn">Open</a>
    <% }) %>
  </ul>

  <a href="/new">
    <button type="button">New Message</button>
  </a>

</body>
</html>